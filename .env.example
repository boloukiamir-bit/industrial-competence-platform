# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://bmvawfrnlpdvcmffqrzc.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9
# Server-side only (never expose to client):
# SUPABASE_SERVICE_ROLE_KEY=...

# Site URL for invite/reset redirects (e.g. https://yourapp.com)
NEXT_PUBLIC_SITE_URL=https://bmvawfrnlpdvcmffqrzc.supabase.co

# Pilot mode: set to "false" or omit for full admin menu (Cockpit, Line Overview, Admin, HR Tasks, etc.)
# Set to "true" for reduced pilot menu (Employees, Compliance, 4 HR items only)
# NEXT_PUBLIC_PILOT_MODE=false

# Cron: set CRON_SECRET and send as x-cron-secret (or Bearer) when calling /api/cron/* endpoints
# CRON_SECRET=dev-secret

# Debug: set to "true" to enable diagnostic logging (auth, org_id, query path, error codes). No PII.
# DEBUG_DIAGNOSTICS=false

# SSL for DATABASE_URL is configured in lib/db.ts (rejectUnauthorized: false for Supabase pooler).
# Do not set NODE_TLS_REJECT_UNAUTHORIZED.
