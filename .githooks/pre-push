#!/usr/bin/env bash
# Pre-push hook: run full verify (build + cockpit smoke) so we don't push broken builds.
# Enable with: git config core.hooksPath .githooks
set -e
cd "$(git rev-parse --show-toplevel)"
npm run verify
